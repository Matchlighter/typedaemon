
import { optional_config_decorator } from "@matchlighter/common_library/cjs/decorators/utils"

export class HomeAssistantIntegration {
    for_event(event: string) {

    }
}

// const sensor = optional_config_decorator([], () => {

// });
